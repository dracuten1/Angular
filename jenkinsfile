pipeline {
  agent {
     docker { image 'node:latest' }
  }
  stages {
    stage('') {
      steps {
        sh 'npm install -g @angular/cli'
        sh 'npm install'
      }
    }

    stage('Build') {
      steps {
        sh 'npm run build'
      }
    }

  }
}
